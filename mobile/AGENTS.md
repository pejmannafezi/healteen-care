# Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/v54.0.0/ before writing any code.
(Pinned to SDK 54 because the iOS App Store Expo Go currently supports SDK 54; 55/56 are pending Apple review.)
